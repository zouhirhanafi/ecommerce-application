apply plugin: "war"

bootWar {
    mainClassName = "ma.ids.ecommerce.EcommerceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
